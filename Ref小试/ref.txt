ref的三种用法： 
1、ref加在普通元素上，用this.ref.name获取到的是dom元素
2、ref加在子组件上，用this.ref.name获取到的是组件实例
3、如何利用v-for和ref获取一组数组或者dom节点

关于ref注册时间的说明： 因为ref本身是作为渲染结果被创建的，在厨师渲染的时候你不能访问它们，它们还不存在！
$refs也不是响应式的，因此不该试图用它在模版中做数据绑定